test: <name>
select the last layer in the layer editor
select the 'Tasks' tab in the layer editor nav bar
    content related to creating layouts is now visible
Assert the save button is disabled.
select the button labeled 'New Layout' in the canvas
    the canvas has a new layout card
    empty canvas instructions are removed.
click on the 'Form' tab in the layer editor nav bar
    a modal is visible describing that Layouts have unsaved changes
click on the discard changes button in the modal
    the form tab is selected and the form builder is visible


test: <name2>
dfa
